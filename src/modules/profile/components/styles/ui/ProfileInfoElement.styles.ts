import styled from "@emotion/styled";

export const ProfileInfoElementWrapper = styled.div`
   display: flex;
   align-items: center;
   gap: 8px;
   padding: 8px;
   border-bottom: ${({ theme }) => theme.borders.base};
`;
