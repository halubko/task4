import styled from "@emotion/styled";

export const PostCommentsListWrapper = styled.div`
   display: flex;
   flex-direction: column;
   padding: 8px;
`;

export const PostCommentsListHorizLine = styled.hr`
   margin: 8px 0;
   opacity: 0.5;
`;
